# AI SDLC — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages

# LLM providers
anthropic>=0.40.0
openai>=1.0.0        # Used for Groq + Together (OpenAI-compatible APIs) + embeddings

# Data backends
asyncpg>=0.29.0      # Async Postgres driver
neo4j>=5.0.0         # Neo4j official async driver
redis>=5.0.0         # Redis async client (redis.asyncio)

# Config + utilities
PyYAML>=6.0.1
PyGithub>=2.3.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
